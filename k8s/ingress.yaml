apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: identity-recon-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: identity-recon
            port:
              number: 80
